<!-- 多文件编程 -->


.h文件放在.c文件同一个文件夹下，
在没有设置include path的情况下，
采用""包含头文件可以正常调用头文件里声明的内容；

.h文件放在.c文件不放在同一个文件夹下，
在没有设置include path的情况下，
采用""或者<>包含头文件都不能正常调用头文件声明里的内容（除非在""中写出相对路径）；

设置include path的情况下，
采用""或者<>包含头文件都能正常调用头文件声明里的内容。