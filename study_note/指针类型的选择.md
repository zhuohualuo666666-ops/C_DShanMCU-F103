<!-- 指针类型的选择 -->

在32位单片机中，对于指针类型：
int*类型指针本身的大小为4字节，32bit，所指向的变量大小为4字节，32bit
char*类型指针本身的大小为4字节，32bit，所指向的变量大小为1字节，8bit

在访问32位单片机的寄存器时，int*类型的指针可以完整读写4个字节，
而char*类型指针只能一个字节一个字节地读写。
无论是从原子性操作32位数据的要求还是代码简洁度上，32位单片机通常都选择int*类型指针